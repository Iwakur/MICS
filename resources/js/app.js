/*
|--------------------------------------------------------------------------
| Frontend entrypoint
|--------------------------------------------------------------------------
|
| The current application does not rely on custom browser-side behavior yet.
| Vite still needs this entry file because @vite(...) injects both the CSS and
| JavaScript entrypoints together.
|
*/
